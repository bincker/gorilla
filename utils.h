#include <iostream>
#include <sstream>
#include <fstream>
#include <ctype.h>
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>

#ifndef UTILS_H
#define UTILS_H

std::string f_to_string(float);
std::string jpopen(std::string, bool);

#endif